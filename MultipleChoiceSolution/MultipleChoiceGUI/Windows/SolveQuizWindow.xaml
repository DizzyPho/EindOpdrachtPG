<Window x:Class="MultipleChoiceGUI.Windows.SolveQuizWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultipleChoiceGUI.Windows"
        mc:Ignorable="d"
        Title="Test Invullen" Height="800" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Label Content="{Binding Title}" HorizontalAlignment="Center" Grid.Row="0" Grid.ColumnSpan="2" Margin="8"></Label>
        <Label Content="Vul je ID in:" HorizontalAlignment="Center" Grid.Row="1" Grid.Column="0" Margin="8"></Label>
        <TextBox Text="{Binding UserId}" Grid.Row="1" Grid.Column="1" Margin="11,8,0,8" HorizontalAlignment="Left" MinWidth="40"></TextBox>
        <GroupBox Header="Test" Margin="8" Grid.Row="2" Grid.ColumnSpan="2">
            <ScrollViewer>
                <ItemsControl Margin="8" BorderThickness="0" Background="Transparent" ItemsSource="{Binding Questions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Label Content="{Binding QuestionText}"></Label>
                                <ItemsControl BorderThickness="0" Background="Transparent" ItemsSource="{Binding Answers}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding AnswerText}" IsChecked="False"></CheckBox>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>
        <Button Grid.Column="0" Grid.Row="3" Margin="8" Content="Terug" Command="{Binding CloseCommand}"></Button>
        <Button Grid.Column="1" Grid.Row="3" Content="Indienen" Margin="8" HorizontalAlignment="Right" Padding="32 4" Command="{Binding SubmitCommand}"></Button>
    </Grid>
</Window>
